import 'reflect-metadata';

const port = process.env.PORT || 4000;


// io.on('connection', socket => {
//  
// });
//
// server.listen(port);
//
// database()
//   .then(() => {
//     server.listen(port);
//   });
